# open-claw-backup
Self-hosted backup pipeline for an OpenClaw server — an n8n workflow that pulls a compressed backup over SSH, uploads it to Google Drive with 2-copy retention, and reports success/failure to Telegram.
